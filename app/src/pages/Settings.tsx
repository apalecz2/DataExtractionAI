export default function Session(): React.ReactElement {
    return (
        <main className="relative h-full overflow-hidden bg-background p-10">
            <div className="space-y-4">
                <h1 className="font-display-lg text-primary">Settings</h1>
            </div>
        </main>
    );
}